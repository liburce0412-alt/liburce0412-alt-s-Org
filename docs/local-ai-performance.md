# Qwen3.5-2B MNN 真机性能报告

日期：2026-08-22。状态：**已完成基础真机验证，正式性能门禁未完成**。

设备：Xiaomi 2410DPN6CC、Android 16、arm64-v8a、约 15 GB RAM；MNN 3.6.1、CPU、4 线程。设备内活跃模型目录约 1.2 GiB，覆盖安装后模型仍保持 Ready。

| 指标 | 目标 | 当前实测 |
|---|---:|---:|
| 冷启动模型加载 | 记录实际值 | 未测 |
| 首 token | 尽量约 3 秒 | 未测 |
| decode token/s | 记录实际值 | 未测 |
| 运行后 TOTAL PSS | 记录实际值 | 1,795,083 kB（含 190,373 kB swap PSS；不是严格峰值） |
| 一次短回复端到端 | 记录实际值 | 约 16.1 秒（包含多次 UIAutomator 轮询，不等同纯推理耗时） |
| 5 分钟温度/稳定性 | 无崩溃、记录温升 | 未测 |

飞行模式下，“今日洞察”基于 85/240 分钟返回了完整中文结论和行动，未暴露思考过程。该测试证明了真实本地链路，不足以推导首 token、decode token/s 或固定 100-token 指标。

代码会在每次本地生成后只记录非内容指标：设备、CPU 后端、4 线程、加载时间、首 token、decode token/s、峰值 PSS、输出 token、总耗时和电池温度起止值。不得把该自动记录替代正式的五分钟稳定性测试。

正式验收必须记录设备型号、Android 版本、总内存、MNN 3.6.1、CPU 后端和线程数；执行冷启动、固定 100-token 提示、连续五分钟中文质量集，并同时做 LOCAL 网络抓包。达不到目标时原样记录，不调整或编造数据。

## Caesar Eval V1

2B/4B 真模型对比入口已定义，当前状态为 **等待首次真机回放，不预填任何结果**。

回放固定使用同一顺序的 50 条合成样本，覆盖中文质量、UTF-8、多轮、图片、只选择不执行的 App/健康工具以及合成健康上下文；分别记录可见 TTFT、native 首 token 原始值、总时长、decode token/s、完成/错误、工具匹配和严格回归分。完整的安全边界、运行命令和报告 schema 见 [Caesar Eval V1](caesar-eval.md)。
